name: Comon
description: Common setup
runs:
  using: "composite"
  steps:
    - name: Import environment variables
      shell: bash
      run: |
        cat ".github/.env" >> "$GITHUB_ENV"
        echo "E2E_SKIP_BUILD=true" >> "$GITHUB_ENV"
        echo "TEST_CHART_SKIP_CHART_BUILD=true" >> "$GITHUB_ENV"
