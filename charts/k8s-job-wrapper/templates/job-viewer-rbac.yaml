apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "k8s-job-wrapper.fullname" . }}-job-viewer-role
  labels:
  {{- include "k8s-job-wrapper.labels" . | nindent 4 }}
rules:
- apiGroups:
  - pixiv.net
  resources:
  - jobs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - pixiv.net
  resources:
  - jobs/status
  verbs:
  - get
