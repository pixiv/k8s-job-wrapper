apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-job-wrapper.fullname" . }}-controller-manager-metrics-service
  labels:
    control-plane: controller-manager
  {{- include "k8s-job-wrapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    app.kubernetes.io/name: k8s-job-wrapper
    control-plane: controller-manager
    {{- include "k8s-job-wrapper.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.metricsService.ports | toYaml | nindent 2 }}
